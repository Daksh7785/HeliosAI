version: '3.8'

services:
  heliosai:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    networks:
      - helios_network

networks:
  helios_network:
    driver: bridge
