[project]
name = "processing"
version = "0.1.0"
description = "HeliosAI Processing Layer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "celery",
    "redis",
    "sqlalchemy",
    "psycopg2-binary",
    "pydantic",
    "pandas",
    "scipy",
    "numpy",
]
