[project]
name = "ingestion"
version = "0.1.0"
description = "HeliosAI Ingestion Layer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "pydantic",
    "celery",
    "sqlalchemy",
    "psycopg2-binary"
]
