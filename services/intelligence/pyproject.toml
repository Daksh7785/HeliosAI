[project]
name = "intelligence"
version = "0.1.0"
description = "HeliosAI Intelligence Layer"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mlflow",
    "scikit-learn",
    "pandas",
    "numpy",
    "sqlalchemy",
    "pydantic",
    "celery",
]
