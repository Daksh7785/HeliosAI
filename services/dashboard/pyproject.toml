[project]
name = "dashboard"
version = "0.1.0"
description = "HeliosAI Plotly Dash Dashboard"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dash",
    "dash-bootstrap-components",
    "plotly",
    "pandas",
    "requests",
    "pydantic",
]
