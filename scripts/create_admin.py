import os
import sys

def create_admin():
    print("Bootstrapping admin user...")
    # Mocking db insertion for initial admin
    print("Admin user 'admin' created with default credentials.")
    print("Please change the password upon first login.")

if __name__ == "__main__":
    create_admin()
